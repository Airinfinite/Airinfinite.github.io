<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>Git 快速入门指南</title>
      <link href="/2022/06/12/git_quick_guide/"/>
      <url>/2022/06/12/git_quick_guide/</url>
      
        <content type="html"><![CDATA[<h2 id="安装并克隆第一个仓库"><a href="#安装并克隆第一个仓库" class="headerlink" title="安装并克隆第一个仓库"></a>安装并克隆第一个仓库</h2><h3 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h3><p>在使用 Git 前，我们需要确保自己的电脑已经安装好 Git。<br>Git 官方下载地址为：<a href="http://git-scm.com/downloads">http://git-scm.com/downloads</a><br>具体的安装步骤可参考 Git 详细指南。</p><p>在终端输入命令 <code>git</code> ，若出现如下提示则证明安装成功。<br><img src="/../img/git_quick_guide.image/Xnip2022-06-07_22-51-20.jpg"></p><h3 id="克隆第一个仓库"><a href="#克隆第一个仓库" class="headerlink" title="克隆第一个仓库"></a>克隆第一个仓库</h3><p>首先我们需要去到我们的 GitHub 仓库，复制 URL。<br><img src="/../img/git_quick_guide.image/Xnip2022-06-07_22-55-36.jpg"><br>在 2021.08.13 之后，输入账号密码的方式来向 GitHub 仓库提交代码等行为不再被支持。我们必须使用 Personal access token 的方式来替代。因此我们需要生成一个 token 以便我们能将 GitHub 中的仓库克隆到本地。我们去到 Settings 中找到 Developer settings。点击 Generate new token。<br><img src="/../img/git_quick_guide.image/Xnip2022-06-07_22-59-08.jpg"><br>在 Note 这一栏输入这个 token 的用途。<br><img src="/../img/git_quick_guide.image/Xnip2022-06-07_23-02-48.jpg"><br>Expiration 是这个 token 的有效期，我们可以根据具体的需求设置。为了方便起见，我们直接勾选所有的 scope，然后点击 Generate token。<br><img src="/../img/git_quick_guide.image/Xnip2022-06-07_23-04-22.jpg"><br>生成好 token 后，我们必须第一时间将其复制保存，因为它只向我们展示一次，之后我们是不能再次查看的。</p><p>在我们有了仓库的 URL 和 token 后，我们需要将两者按以下规则拼接在一起。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">https://oauth2:token@github.com/username/xxx.git</span><br></pre></td></tr></table></figure><p>其实就是在 URL 中插入 <code>oauth2:token@</code> 。接着我们在终端运行一下命令：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git clone https://oauth2:token@github.com/username/xxx.git</span><br></pre></td></tr></table></figure><p>如果一切顺利，我们就可以在你打开终端的目录中看到与你仓库同名的文件夹。<br><img src="/../img/git_quick_guide.image/Xnip2022-06-07_23-17-47.jpg"></p><p>到这里，我们就完成了 Git 的安装，并且已经会将自己 GitHub 中的仓库克隆到本地。下面，我们将简单介绍一下 Git 的工作流程，以便让我们能在使用 Git 上得心应手。</p><h2 id="Git-工作流程及基本概念"><a href="#Git-工作流程及基本概念" class="headerlink" title="Git 工作流程及基本概念"></a>Git 工作流程及基本概念</h2><h3 id="工作流程"><a href="#工作流程" class="headerlink" title="工作流程"></a>工作流程</h3><p>Git 的一般工作流程如下：</p><ol><li>将远程仓库克隆到本地</li><li>从克隆的本地仓库中检出一条分支到工作区，对分支进行修改</li><li>在我们对分支修改完成后，先将其添加到本地缓存区</li><li>添加到本地缓存区后，我们可以提交修改后的分支到本地仓库</li><li>最后，我们可以将本地仓库推送到远程仓库<br><img src="/../img/git_quick_guide.image/Xnip2022-06-08_07-53-37.jpg"></li></ol><h3 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h3><ul><li>Remote 是远程仓库，常用的有 GitHub、Gitee 等。</li><li><code>clone</code>：将远程仓库克隆到本地仓库。</li><li>Repository 是本地仓库，也就是版本库，工作区的 <code>.git</code> 文件夹。在这里存放着我们代码的各个历史版本。</li><li><code>checkout</code>：从本地仓库中检出一个分支然后进行修改。</li><li><code>workspace</code>：工作区，也就是我们能直接看得见的文件夹。</li><li><code>add</code>：在提交代码前，现将代码添加到暂存区。</li><li>Index 一般存放在 <code>.git</code> 目录下的 index 文件（.git&#x2F;index）中，所以我们把暂存区有时也叫作索引。</li><li><code>commit</code>：将我们添加到暂存区的代码提交到本地仓库。</li><li><code>push</code>：将本地仓库推送到远程仓库。</li></ul><h2 id="创建仓库"><a href="#创建仓库" class="headerlink" title="创建仓库"></a>创建仓库</h2><p>在这里，我们将介绍如何创建 Git 仓库。主要有以下两种方式：</p><ol><li>在本地创建 Git 仓库</li><li>将远程仓库克隆到本地</li></ol><h3 id="在本地创建-Git-仓库-——-git-init"><a href="#在本地创建-Git-仓库-——-git-init" class="headerlink" title="在本地创建 Git 仓库 —— git init"></a>在本地创建 Git 仓库 —— git init</h3><p>Git 使用 <code>git init</code> 命令来初始化一个 Git 仓库，Git 的很多命令都需要在 Git 的仓库中运行，所以 <code>git init</code> 是使用 Git 的第一个命令。<br>在执行完成 <code>git init</code> 命令后，会在当前文件夹 生成一个 .git 目录，也就是本地仓库。</p><h4 id="使用方法"><a href="#使用方法" class="headerlink" title="使用方法"></a>使用方法</h4><p>使用当前目录作为 Git 仓库，我们只需使它初始化。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git init</span><br></pre></td></tr></table></figure><p>该命令执行完后会在当前目录生成一个 .git 目录。<br>或者我们可以指定特定目录作为 Git 仓库。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git init [newrepo]</span><br></pre></td></tr></table></figure><p>初始化后，会在 newrepo 目录下会出现一个名为 .git 的目录，所有 Git 需要的数据和资源都存放在这个目录中。<br>如果当前目录下有几个文件想要纳入版本控制，需要先用 <code>git add</code> 命令告诉 Git 开始对这些文件进行跟踪，然后提交：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">$ git add *.c</span><br><span class="line">$ git add README</span><br><span class="line">$ git commit -m &#x27;版本说明&#x27;</span><br></pre></td></tr></table></figure><p>以上命令将目录下以 .c 结尾及 README 文件提交到仓库中。</p><h3 id="将远程仓库克隆到本地-——-git-clone"><a href="#将远程仓库克隆到本地-——-git-clone" class="headerlink" title="将远程仓库克隆到本地 —— git clone"></a>将远程仓库克隆到本地 —— git clone</h3><p>我们使用 <code>git clone</code> 从远程 Git 仓库中拷贝项目。克隆仓库的命令格式为：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git clone [repo]</span><br></pre></td></tr></table></figure><p>如果我们需要克隆到指定的目录，可以使用以下命令格式：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git clone [repo] [directory]</span><br></pre></td></tr></table></figure><p>参数说明：</p><ol><li>repo：远程 Git 仓库</li><li>directory：本地目录</li></ol><p>比如，要克隆 Ruby 语言的 Git 代码仓库 Grit，可以用下面的命令：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git clone git://github.com/schacon/grit.git</span><br></pre></td></tr></table></figure><p>执行该命令后，会在当前目录下创建一个名为 grit 的目录，其中包含一个 .git 的目录，用于保存下载下来的所有版本记录。<br>如果要自己定义要新建的项目目录名称，可以在上面的命令末尾指定新的名字：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git clone git://github.com/schacon/grit.git mygrit</span><br></pre></td></tr></table></figure><h2 id="基本操作"><a href="#基本操作" class="headerlink" title="基本操作"></a>基本操作</h2><p>Git 的工作就是创建和保存你项目的快照及与之后的快照进行对比。本章将对有关创建与提交你的项目快照的命令作介绍。<br>Git 常用的是以下 6 个命令：<code>git clone</code>、<code>git push</code>、<code>git add</code>、<code>git commit</code>、<code>git checkout</code>、<code>git pull</code>，接下来我们将一一介绍。<br><img src="/../img/git_quick_guide.image/2022-06-08-10-01-01.png"></p><h3 id="创建仓库命令"><a href="#创建仓库命令" class="headerlink" title="创建仓库命令"></a>创建仓库命令</h3><h4 id="git-init"><a href="#git-init" class="headerlink" title="git init"></a>git init</h4><p><code>git init</code> 命令用于在目录中创建新的 Git 仓库。<br>在目录中执行 <code>git init</code> 就可以创建一个 Git 仓库了。<br>例如，我们在当前目录下创建一个名为 note 的项目：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">mkdir note</span><br><span class="line">cd note/</span><br><span class="line">git init</span><br><span class="line"># Initialized empty Git repository in ~/Desktop/note/.git/</span><br></pre></td></tr></table></figure><p>现在你可以看到在你的项目中生成了 .git 这个子目录，这就是你的 Git 仓库了，所有有关你的此项目的快照数据都存放在这里。</p><h4 id="git-clone"><a href="#git-clone" class="headerlink" title="git clone"></a>git clone</h4><p><code>git clone</code> 拷贝一个 Git 仓库到本地，让自己能够查看该项目，或者进行修改。拷贝项目命令格式如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git clone [URL]</span><br></pre></td></tr></table></figure><p>URL 是我们要拷贝的 GitHub 上的项目。</p><p>默认情况下，Git 会按照你提供的 URL 所指向的项目的名称创建你的本地项目目录。通常就是该 URL 最后一个 &#x2F; 之后的项目名称。如果你想要一个不一样的名字，你可以在该命令后加上你想要的名称。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git clone [URL] [reopname]</span><br></pre></td></tr></table></figure><p>reponame 就是你想要自定义的名字。</p><h3 id="提交与修改"><a href="#提交与修改" class="headerlink" title="提交与修改"></a>提交与修改</h3><h4 id="git-add"><a href="#git-add" class="headerlink" title="git add"></a>git add</h4><p><code>git add</code> 命令可将该文件添加到暂存区。</p><p>添加一个或多个文件到暂存区：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git add [file1] [file2] ...</span><br></pre></td></tr></table></figure><p>添加指定目录到暂存区，包括子目录：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git add [dir]</span><br></pre></td></tr></table></figure><p>添加当前目录下的所有文件到暂存区：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git add .</span><br></pre></td></tr></table></figure><h4 id="git-commit"><a href="#git-commit" class="headerlink" title="git commit"></a>git commit</h4><p><code>git commit</code> 命令将暂存区内容添加到本地仓库中。<br>提交暂存区到本地仓库中:</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git commit -m [message]</span><br></pre></td></tr></table></figure><p>message 可以是一些备注信息。<br>提交暂存区的指定文件到仓库区：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git commit [file1] [file2] ... -m [message]</span><br></pre></td></tr></table></figure><h3 id="远程操作"><a href="#远程操作" class="headerlink" title="远程操作"></a>远程操作</h3><h4 id="git-push"><a href="#git-push" class="headerlink" title="git push"></a>git push</h4><p><code>git push</code> 命令将本地的分支版本上传到远程并合并。命令格式如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git push &lt;远程主机名&gt; &lt;本地分支名&gt;:&lt;远程分支名&gt;</span><br></pre></td></tr></table></figure><p>如果本地分支名与远程分支名相同，则可以省略冒号：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git push &lt;远程主机名&gt; &lt;本地分支名&gt;</span><br></pre></td></tr></table></figure><h5 id="实例"><a href="#实例" class="headerlink" title="实例"></a>实例</h5><p>以下命令将本地的 master 分支推送到 origin 主机的 master 分支。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git push origin master</span><br></pre></td></tr></table></figure><p>相等于：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git push origin master:master</span><br></pre></td></tr></table></figure><h4 id="git-pull"><a href="#git-pull" class="headerlink" title="git pull"></a>git pull</h4><p><code>git pull</code> 命令用于从远程获取代码并合并本地的版本。<br><code>git pull</code> 其实就是 <code>git fetch</code> 和 <code>git merge FETCH_HEAD</code> 的简写。命令格式如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git pull &lt;远程主机名&gt; &lt;远程分支名&gt;:&lt;本地分支名&gt;</span><br></pre></td></tr></table></figure><h5 id="实例-1"><a href="#实例-1" class="headerlink" title="实例"></a>实例</h5><p>将远程主机 origin 的 master 分支拉取过来，与本地的 brantest 分支合并。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git pull origin master:brantest</span><br></pre></td></tr></table></figure><p>如果远程分支是与当前分支合并，则冒号后面的部分可以省略。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git pull origin master</span><br></pre></td></tr></table></figure>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2022/06/11/HelloWorld/"/>
      <url>/2022/06/11/HelloWorld/</url>
      
        <content type="html"><![CDATA[<h1 id="Hello-World"><a href="#Hello-World" class="headerlink" title="Hello World"></a>Hello World</h1>]]></content>
      
      
      
    </entry>
    
    
  
  
</search>
